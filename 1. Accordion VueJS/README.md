Приложение с компонентом Аккордеон на Vue JS.

```npm i```

```npm run serve```

```http://localhost:8000```

**Warning**

Для запуска приложения с локального окружения необходимо запустить браузер в режиме `--disable-web-security` для работы с CORS. 
